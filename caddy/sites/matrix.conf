# Matrix homeserver (Client-Server API)
# Import from machine-wide Caddyfile.global — reload only, never pkill caddy.
#
# matrix.bevel.is {
#   import common_headers
#   reverse_proxy 127.0.0.1:8008
#   log {
#     output file /var/log/caddy/matrix_bevel_is.log {
#       roll_size 10MB
#       roll_keep 5
#     }
#     format json
#   }
# }

# Placeholder for monorepo docs; production block lives in /etc/caddy/Caddyfile
# on bevel-prod after Synapse is installed.

# BEVEL realtime (WebSocket) — port 43208
realtime.bevel.lvh.me {
	reverse_proxy 127.0.0.1:43208 {
		flush_interval -1
		transport http {
			versions 1.1
			read_timeout 0
			write_timeout 0
			dial_timeout 10s
		}
	}
	log {
		output file {$HOME}/dev/logs/bevel-realtime.log
		format console
	}
}

# Production (EC2 /etc/caddy/Caddyfile) — do not gzip this host; WS frames
# must flush immediately. Reload Caddy, never pkill.
# realtime.bevel.is {
# 	header {
# 		-Server
# 		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
# 		X-Frame-Options "SAMEORIGIN"
# 		X-Content-Type-Options "nosniff"
# 		Referrer-Policy "strict-origin-when-cross-origin"
# 	}
# 	reverse_proxy 127.0.0.1:43208 {
# 		flush_interval -1
# 		transport http {
# 			versions 1.1
# 			read_timeout 0
# 			write_timeout 0
# 			dial_timeout 10s
# 		}
# 	}
# }

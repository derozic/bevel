# BEVEL control plane (FastAPI REST + GraphQL) — port 43203
# Local
api.bevel.lvh.me {
	import devsite 43203 bevel-api
}

# Production (EC2 Caddy) — MUST point at FastAPI, never Next:
# api.bevel.is {
# 	reverse_proxy 127.0.0.1:43203
# }

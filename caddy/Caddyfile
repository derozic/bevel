# Deprecated local runner — do NOT `caddy run` this file.
# BEVEL sites live under caddy/sites/*.conf and are imported by the machine-wide
# ~/dev/Caddyfile.global. Reload that instance instead of starting a second Caddy.
#
#   caddy validate --config ~/dev/Caddyfile.global --adapter caddyfile
#   caddy reload  --config ~/dev/Caddyfile.global --adapter caddyfile
#
# See ~/dev/CADDY_ORCHESTRATION.md

import sites/*.conf

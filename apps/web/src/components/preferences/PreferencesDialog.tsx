'use client'

/**
 * @deprecated Preferences now open as a right-edge panel.
 * Kept as a thin alias so existing imports keep working.
 */
export { PreferencesPanel as PreferencesDialog } from './PreferencesPanel'

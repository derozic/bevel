/**
 * Resolve the BEVEL realtime WebSocket base URL for the current browser host.
 * Re-export package resolver so web never drifts back to legacy :41008 defaults.
 */
export { resolveRealtimeUrl } from '@bevel/realtime-client'

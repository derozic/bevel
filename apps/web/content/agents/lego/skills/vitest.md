# Vitest Skill

Generate unit and component tests for TypeScript, React, Next.js, and Vue.

- Match existing `describe` / `it` / `expect` patterns in the repo
- Use `@testing-library` for component tests
- Mock external APIs at the boundary
- Prefer `vi.fn()` and `vi.mock()` consistent with repo style
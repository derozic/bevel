# pytest Skill

Generate tests for FastAPI, Pydantic models, and Python services.

- Use fixtures from `conftest.py` when present
- Test models, endpoints, and GraphQL resolvers separately
- Target PostgreSQL — never SQLite fallbacks
- Use `uv run pytest` invocation in CI notes
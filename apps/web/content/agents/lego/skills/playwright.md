# Playwright Skill

Generate end-to-end tests for user-facing flows.

- Follow existing `page.goto` and locator patterns
- Cover critical paths introduced or changed in the PR
- Keep tests deterministic — no arbitrary sleeps
- Use data-testid or role selectors when available
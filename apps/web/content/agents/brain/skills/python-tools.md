# Python Tools Skill

Execute Python for documents, data, and automation.

- Use `uv run` when available
- Respect `AGENTS_PYTHON_ROOT` working directory
- Clean up temp files after execution
- Return structured artifacts